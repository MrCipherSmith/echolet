# Flow Journal

- 2026-09-07T06:55:37.619Z - flow created
- 2026-09-07T06:56:43.880Z - task-added: T5: Design the flooding closure and justify the choice
- 2026-09-07T06:56:43.959Z - task-added: T6: RED tests for the chosen flooding closure
- 2026-09-07T06:56:44.036Z - task-added: T7: Implement the flooding closure
- 2026-09-07T06:56:44.114Z - task-added: T8: Serve HTTPS for non-loopback relay URLs
- 2026-09-07T06:56:44.190Z - task-added: T9: Operator console over the local CLI
- 2026-09-07: USER DECISION — the operator surface becomes a **TUI**, not a browser page, with `keryx` on `geekom` at `/home/altsay/keryx/src/tui` as the reference. Inspected before adopting: 77 files, 38 colocated tests, 1.4 MB and **no UI dependency at all** — only Node builtins, so the rendering is hand-rolled ANSI over the alternate screen with raw-mode input. That property is why the reference is worth following here: a hand-rolled renderer is unit-testable line by line, which a framework's virtual DOM would not be, and this flow's method is test-first with independent acceptance. Structure to mirror: `tui-shell` as the shell, `shell-chrome` for frames, `transcript-blocks`, `modal-host` for confirmations, and pluggable inspector panes.
- 2026-09-07: The TUI is also strictly safer than the web console for the property AC5 protects. There is no HTTP server, no browser origin and no localhost port to reach, so a store key or private key cannot leave the operator's terminal even by accident. AC5 becomes easier to satisfy honestly rather than harder, and the `description.md` rationale for rejecting a server-side-keys web client holds a fortiori.
- 2026-09-07: Repository connected to `git@github.com:MrCipherSmith/echolet.git` (private, was empty). GitHub rejected the first push under email-privacy protection because the four commits carried the user's personal address. The protection was NOT disabled — instead the four local, unpushed commits were re-authored to the GitHub noreply address, verified as 0 commits carrying the personal address and 4 carrying noreply, and the repository-local `user.email` was pinned so it cannot recur. Tailscale was started at the user's request; tailnet is `tail5a88fb.ts.net` with MagicDNS, and both relays will use `tailscale cert` names rather than public DNS.
- 2026-09-07T06:56:44.266Z - task-added: T10: Verify the closure and the console on a real relay
- 2026-09-07T06:56:44.347Z - task-added: T11: Deploy two relay instances and write the deployment runbook
- 2026-09-07T06:57:00.427Z - frozen: 8 criteria; checksum recorded
- 2026-09-07T06:57:00.520Z - started
- 2026-09-07T06:57:09.881Z - task-done: T1: Collect remaining context
- 2026-09-07T06:57:09.965Z - task-done: T2: Implement per plan
- 2026-09-07T06:57:10.046Z - task-done: T3: Add/adjust tests and make them pass
- 2026-09-07T06:57:10.132Z - task-done: T4: Self-review and prepare draft PR
- 2026-09-07T06:57:10.236Z - task-depends-set: T6: dependsOn T5 (was empty) — Tests follow the chosen design
- 2026-09-07T06:57:10.319Z - task-depends-set: T7: dependsOn T6 (was empty) — Implementation follows the RED tests
- 2026-09-07T06:57:10.398Z - task-depends-set: T10: dependsOn T7, T8, T9 (was empty) — Verification runs on the closure, TLS and the console together
- 2026-09-07T06:57:10.477Z - task-depends-set: T11: dependsOn T10 (was empty) — Nothing is exposed before the closure is verified — the user fixed this order
- 2026-09-07T06:57:10.556Z - task-attempt: T5: started (attempt 1) — 002-T5-design
- 2026-09-07T07:13:34.648Z - task-done: T5: Design the flooding closure and justify the choice
- 2026-09-07T07:13:34.731Z - task-attempt: T6: started (attempt 1) — 002-T6-tests
- 2026-09-07T07:22:40.753Z - task-attempt: T9: started (attempt 1) — 002-T9-tui-design-and-tests
- 2026-09-07T08:32:12.797Z - task-done: T6: RED tests for the chosen flooding closure
- 2026-09-07T08:32:12.886Z - task-attempt: T7: started (attempt 1) — 002-T7-implement
- 2026-09-07T08:32:12.979Z - task-attempt: T9: started (attempt 2) — 002-T9-implement
- 2026-09-07T09:04:14.679Z - task-done: T7: Implement the flooding closure
- 2026-09-07T09:04:14.758Z - task-done: T9: Operator console over the local CLI
- 2026-09-07T09:04:49.577Z - task-attempt: T8: started (attempt 1) — 002-T8-tls
- 2026-09-07T09:43:36.913Z - task-done: T8: Serve HTTPS for non-loopback relay URLs
- 2026-09-07T09:43:36.999Z - task-attempt: T10: started (attempt 1) — 002-T10-verify
- 2026-09-07T10:26:24.540Z - task-added: T12: Prepare deployment artifacts: relay container image, unit files, runbook draft
- 2026-09-07T10:26:24.625Z - task-depends-set: T11: dependsOn T10, T12 (was T10) — Deployment consumes the prepared artifacts and still waits for verification
- 2026-09-07T10:26:24.708Z - task-attempt: T12: started (attempt 1) — 002-T12-deploy-prep
- 2026-09-07T11:12:53.270Z - task-attempt: T10: failed (attempt 2) — T10-F-001: the contact-import re-walk is itself bounded at 16 pages and its flag is consumed regardless, so a message behind >16 pages of poison is permanently lost
- 2026-09-07T11:12:53.358Z - task-added: T13: RED tests for the truncated contact-import re-walk and the unauthenticated count oracle
- 2026-09-07T11:12:53.448Z - task-added: T14: Fix T10-F-001 message loss, T10-F-002 count oracle, T10-F-003 relay URL path
- 2026-09-07T11:13:01.963Z - task-depends-set: T14: dependsOn T13 (was empty) — Implementation follows the RED tests
- 2026-09-07T11:13:02.060Z - task-depends-set: T10: dependsOn T7, T8, T9, T14 (was T7, T8, T9) — Verification is re-run after the regression it found is fixed
- 2026-09-07T11:13:02.151Z - task-attempt: T13: started (attempt 1) — 002-T13-tests
- 2026-09-07T11:32:56.020Z - task-done: T12: Prepare deployment artifacts: relay container image, unit files, runbook draft
- 2026-09-07T11:34:31.770Z - task-done: T13: RED tests for the truncated contact-import re-walk and the unauthenticated count oracle
- 2026-09-07T11:34:31.853Z - task-attempt: T14: started (attempt 1) — 002-T14-implement
- 2026-09-07T12:05:13.794Z - task-done: T14: Fix T10-F-001 message loss, T10-F-002 count oracle, T10-F-003 relay URL path
- 2026-09-07T12:05:22.969Z - task-attempt: T10: started (attempt 3) — 002-T10-verify re-run after T14
- 2026-09-07: T10 re-dispatched as `002-T10-verify-r2` against a fresh verifier that wrote none of this code. Attempt 2's verdicts are **not** credited retroactively: every AC judgement is remade on the post-T14 tree, and the verifier is told explicitly to attack the re-walk path T14 rewrote rather than to re-read the earlier report. Started in parallel: read-only reconnaissance of `geekom` and `depr` for T11. It is pure queries — nothing installed, started, configured or exposed — so it does not violate the ordering the user fixed, that nothing is exposed before the closure is verified; it only converts T11 from a discovery task into a mechanical one.
- 2026-09-07: Host reconnaissance returned — `t11-host-recon.md`. Both hosts are Ubuntu 24.04.4 amd64, reachable, Docker-ready without sudo, Tailscale `Running` on `tail5a88fb.ts.net` as `geekom.tail5a88fb.ts.net` (100.116.255.111) and `depr.tail5a88fb.ts.net` (100.100.188.64), clocks NTP-synced, **ports 443 and 8443 free on both**, and no echolet artefact of any kind present. Only two template values are unknown to the artefacts and both are now recorded. **One blocker, and it is not on the hosts:** `tailscale status --json` reports `CertDomains: null` on both, i.e. HTTPS Certificates are not enabled for the tailnet, so `tailscale cert` will fail and the relay — which never degrades to plain HTTP — cannot start. That is a toggle in the Tailscale admin console (DNS → HTTPS Certificates) owned by the tailnet owner; it cannot be done over SSH and I am not doing it silently. The deployment runbook does not name this prerequisite, which is a defect in the runbook that T11 must fix. Second-order: `geekom`'s `altsay` has password-gated sudo, so its privileged steps need an interactive session, while `depr`'s `ubuntu` is `NOPASSWD` and scriptable.
- 2026-09-07: T10 attempt 3 returned **DONE_WITH_CONCERNS** — AC1, AC2, AC3, AC5, AC6 met, AC4 partial for the one half loopback cannot establish, matrix green (278 tests, e2e 24/24, `test:e2e` 3/3, Go untagged + `-race` + `-race -tags=relayv2`, 0 data races), integrity 242/242 by SHA-256, and the T14 fixes confirmed by measurement: the exact 5×55 case that lost a message now delivers on the 2nd post-import poll, 166 envelopes on the 4th, 5006 on the 7th, always `ceil(pages/16)` polls, amplification 1.0006:1 paid once.
- 2026-09-07: **I declined to accept it.** T10R3-F-001 is silent, permanent message loss when a recovery poll is interrupted with **Ctrl-C** — not a crash, the ordinary way an operator stops a poll, and a case `inbound.ts` itself names as normal. The verifier was asked to *state* the first losing configuration rather than fail on it, which is why its own status is DONE_WITH_CONCERNS; the decision to treat it as a blocker rather than an AC8 residual is mine, and the reason is that a messenger which loses a message on Ctrl-C without saying so must not be deployed. Confirmed independently by reading the source rather than trusting the report: `takeMailboxRewalk()` deletes the position before the walk and `keepMailboxRewalk()` writes it back only on return, so the whole walk is a window in which the position exists nowhere durable. Carrying T10R3-F-002 (the ack route still answers the lifetime count to a contact-card holder — T14 fixed the poll route only) and T10R3-F-003 (`RelayClient`'s `/^127\./` admits `127.evil.example` as loopback) into the same fix. T15 (RED) and T16 (implement) added; T10 re-pointed onto T16 and, again, not credited retroactively.
- 2026-09-07: The design trap for T15/T16, recorded so the fix cannot swap one defect for a worse one: delete-before-the-walk was deliberate, per finding T6-F-004 — a position surviving its own **successful** walk restarts every later poll at the same place and reinstates the whole flooding class. Both properties must hold simultaneously, so the RED set pins both, and the intended shape is peek-and-advance rather than take-and-restore: never remove the position before the walk, advance it to where the walk actually reached, clear it only when the walk reaches the end of the mailbox.
- 2026-09-07: T15 delivered 17 tests — 8 RED, 9 green guards — and escalated two questions. The interruption is not timed: a counting loopback proxy HOLDS the third poll page's response and the signal is delivered while the CLI is provably blocked on it, inside the window `takeMailboxRewalk` opened, and the held request is then dropped so nothing moves relay state behind the test. The T6-F-004 guard is green by design, which is what makes "never clear the position" an unacceptable fix.
- 2026-09-07: **ORCHESTRATOR RULING on T15-F-001.** `flood-closure` RED-4 asserts the poll after a SIGKILL is cursorless, and a crash-safe re-walk held as a durable local position necessarily makes it cursored. T14 recorded this as its reason not to fix the defect at all. I authorise re-stating that single assertion, and I reject the alternative of moving the re-walk floor onto the relay. Two reasons. First, the assertion is green *because of the defect*: `takeMailboxRewalk` erases the position before the walk, so the SIGKILL leaves nothing to present; an assertion that holds only while the bug is present protects nothing and would force the fix to be wrong. Second, the relay is the adversary-adjacent component and this design deliberately keeps the recipient's recovery decision local — `contact import` is an offline trust operation making no relay request — so putting the recovery position on the relay would hand the flooding adversary a lever on the very path that recovers from flooding. The replacement must be strictly stronger and must assert the property rather than the mechanism: a poll with **no** re-walk pending must still be cursorless (F-012 / T6-F-005 stays pinned exactly where it still holds), and a cursor that *is* presented must name a position this client actually judged, never beyond `lastJudged`, which is the real R-4 hazard the cursorless line was standing in for. Only that one line may change.
- 2026-09-07: **RULING on T15-F-002.** T15 proposed answering every signature-invalid ack with a uniform `400 INVALID_SCHEMA`. Rejected as unnecessary: the malformed-shapes test demands only that *shape* validation precede authorisation, and `TestAckRefusesAReadThroughAboveAnyPositionTheRelayIssued` signs correctly, so the same split T14 applied to the poll route — shape before authorisation, range only after `VerifyMessageSignature` — gives one uniform answer to an unverified caller and keeps both existing tests green without a special case.
- 2026-09-07: Environment trap worth recording, because it cost me a run and reads exactly like broken code: `bash -lc` on this machine starts **Node v22.12.0**, which lacks `node:sqlite`, so `packages/session-node` fails to collect and the suite aborts before `apps/cli` is reached. The default non-login interpreter is v26.5.0. The runbook already warns about this Node boundary in the abstract; what it does not say is that the login shell on the development machine itself is on the wrong side of it.
- 2026-09-07: RED-4's cursorless line was re-stated as authorised — exactly one assertion removed, every other RED-4 expectation byte-identical — and replaced by three that pin the property instead of the mechanism: a presented cursor must be a token the relay ISSUED to the killed process for a page it actually received (`null` and the head `"0"` admitted, tokens compared for equality only so no encoding is constrained); the resumed walk's first page must start at or before `lastOffered + 1`, which binds the cursorless path too; and a precondition that at least two continuation tokens were issued, so the first can never pass vacuously. The narrow exception became its own case RED-4b: after proving from the relay's own `next_cursor` that the re-walk ran to the END of the mailbox, an ordinary poll must present no cursor. RED-4b is also the cursor-shaped T6-F-004 guard — "never clear the re-walk" turns it red — and it is deliberately cheap at 3.0 s with no flood.
- 2026-09-07: **My ruling on T15-F-002 rested on a false premise and T15 caught it.** I wrote that `TestAckRefusesAReadThroughAboveAnyPositionTheRelayIssued` would stay green under the shape/range split "because it signs correctly". It does not sign correctly. `ackWithReadThrough` is deliberately the ATTACK helper — its own comment says it signs the transcript that exists today, `(recipient_mailbox_id, device_id, envelope_ids)`, precisely so that `read_through` is invisible to the signature — while the handler builds the V2 transcript whenever `read_through` is present. That test is green today only because the range bound runs before authentication, which is the defect. I verified this myself in the source before ruling again. Resolution: a second, legitimate-client helper that signs V2, used in that one test, with **every assertion left byte-identical**. `ackWithReadThrough` keeps signing V1 because `TestAckSignatureMustBindReadThrough` depends on that being an attack. The change makes the test finally exercise what its own comment claims — a buggy but honest client refused with 400 — instead of passing because the bound preceded authentication. Checked each of the other three call sites: line 113 asserts only "not 200" and "some bounded 4xx" so 403 keeps it green; line 153 is answered by the shape gate before authorisation; line 203 sends no `read_through` and so builds V1 and verifies.
- 2026-09-07T14:51:33.706Z - task-attempt: T10: failed (attempt 4) — Verifier returned DONE_WITH_CONCERNS, but the orchestrator declines to accept: T10R3-F-001 (major) is silent permanent message loss when a recovery poll is interrupted with Ctrl-C, which is a normal way to stop a poll and is not an acceptable residual before deployment. Confirmed by reading the source, not only from the report: takeMailboxRewalk() deletes the position before the walk and it is written back only on return, so the whole walk is a crash window. Also carrying T10R3-F-002 (ack-route count oracle) and T10R3-F-003 (RelayClient loopback hostname rule) into the fix.
- 2026-09-07T14:51:33.790Z - task-added: T15: RED tests for re-walk crash safety, the ack-route count oracle and the loopback hostname rule
- 2026-09-07T14:51:33.940Z - task-added: T16: Fix T10R3-F-001 re-walk crash safety, T10R3-F-002 ack oracle, T10R3-F-003 loopback rule
- 2026-09-07T14:51:40.548Z - task-depends-set: T16: dependsOn T15 (was empty) — Implementation follows the RED tests
- 2026-09-07T14:51:40.634Z - task-depends-set: T10: dependsOn T7, T8, T9, T14, T16 (was T7, T8, T9, T14) — Verification is re-run after the crash-safety defect it found is fixed; attempt 3 is not credited retroactively
- 2026-09-07T14:51:40.718Z - task-attempt: T15: started (attempt 1) — 002-T15-tests
- 2026-09-07T15:54:46.726Z - task-done: T15: RED tests for re-walk crash safety, the ack-route count oracle and the loopback hostname rule
- 2026-09-07T15:54:46.811Z - task-attempt: T16: started (attempt 1) — 002-T16-implement
- 2026-09-07T16:20:33.618Z - task-done: T16: Fix T10R3-F-001 re-walk crash safety, T10R3-F-002 ack oracle, T10R3-F-003 loopback rule
- 2026-09-07T16:20:33.700Z - task-attempt: T10: started (attempt 5) — 002-T10-verify-r3 re-run after T16
