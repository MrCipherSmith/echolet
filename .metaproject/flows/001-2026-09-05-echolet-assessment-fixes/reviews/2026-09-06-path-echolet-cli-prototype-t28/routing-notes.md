# T28 review routing

Version: 0.1.0

The managed path review retains 98 explicitly enumerated source/test/package files. `keryx review scope --path` is a pre-filter, not directory discovery: candidate directories were expanded to files before saving scope.json. No Git revision exists; source-hashes-before.json identifies the observed files without inventing a commit.

The initial budget calculation used cap 4 and 2 enclosing agents, yielding two worker slots. Runtime repeatedly refused a second reviewer with `agent thread limit reached`, including reuse of completed root workers. Parent authorized sequential reuse of one child for five domain roles, each with a separate input, result and report. This is five role passes by one worker actor, not five independent actors. A different actor must perform final finding verification.

Mechanical tiers initially picked stale local rapid-mlx session metadata. They were recomputed with the parent-confirmed hosted OpenAI/GPT-6 anchor. All domain roles resolve standard/session-fallback; execution verifier resolves light/session-fallback, but no hosted catalog is discoverable, so actual workers inherit the hosted session. No rapid-mlx model was dispatched.

All six reviewer inputs were validated against the local Draft 2020-12 schemas using jsonschema installed only under /tmp/echolet-t28-validator. The keryx contract registry does not expose reviewer-input or reviewer-finding; final subagent-result and individual normalized findings use its registered schemas.

Memory search restricted to accepted entries returned zero matches. Wiki index and session module page were read; they are drafts and freshness cannot establish a revision range in the unborn checkout. Approved requirements and source evidence are authoritative. Graph context has 70 nodes and 104 edges from the T27 rebuild.

The initial specification gate mapped AC-01 through AC-10 to source and T27 evidence and found no wholly missing acceptance criterion. Domain defects remain separately reportable. Mutation checks, if run, must use isolated temporary copies; canonical source/tests remain unchanged.

No PR exists, so external comment collection/publication is not applicable. No frontend/MobX/legacy reviewer was selected; no project-local review skills were discovered. Review stack detection is uncertain for this workspace manifest, which was recorded rather than used to suppress the explicitly requested backend role.

Audit: graph_used=yes; wiki_used=yes (draft-only limitation); ctx_used=yes; raw_rg_used=no.
