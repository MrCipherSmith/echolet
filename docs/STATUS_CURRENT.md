# Echolet — текущая готовность

Обновлено: 2026-09-07 (Asia/Muscat). Единственный актуальный статус проекта.

**Стадия: технический прототип на компьютере. Полный защищённый MVP, готовность к чувствительной переписке, работа на мобильных устройствах, готовность к развёртыванию, внешний криптографический аудит и подтверждённый пользовательский спрос не заявляются.**

Управляемая работа: [flow 001](../.metaproject/flows/001-2026-09-05-echolet-assessment-fixes/description.md) и [flow 002](../.metaproject/flows/002-2026-09-07-echolet-close-the-flood-class-operator-c/description.md). Статусы задач ведутся через Keryx CLI.

Ревизия, к которой относится этот статус, — коммит **`c302485`** («fix: the contact-import re-walk survives an interrupted poll»), рабочее дерево чистое. Ограничение, зафиксированное в предыдущей редакции этого файла («ветка `main` — unborn, ни одного коммита и ни одного remote»), **снято**: репозиторий закоммичен и подключён к приватному remote, поэтому измерения ниже привязаны к хешу коммита.

## Что реализовано и проверено локально

| Область | Наблюдаемое состояние / основание | Условие приёмки |
|---|---|---|
| Relay v2: публикация и атомарная выдача bundle | **Реализовано, сетевой путь работает.** Маршруты `POST /v2/prekeys/publish` и `POST /v2/prekeys/claim`; ответ публикации содержит обязательное поле `claimable`. Конкурентная выдача проверена Go-тестами под `-race` | Ровно один победитель из ≥20 конкурентных claim; повтор с тем же `claim_id` возвращает те же байты |
| CLI-прототип на компьютере | **Реализовано и локально проверено.** Восемь команд (`init`, `contact export/import`, `relay publish`, `send`, `poll`, `history`, `doctor`); профили с зашифрованным durable-хранилищем; поверхность команд заморожена | Два независимых процесса обмениваются шифротекстом через relay и переживают рестарт |
| Двухпроцессный end-to-end сценарий | **Реализовано и проходит 3/3** без фильтрации: доставка получателю, который был офлайн; рестарт отправителя с побайтно точным повтором; рестарт получателя без дублей в истории; восстановление после неудавшегося ack | Сценарий выполняется на чистом дереве |
| Аутентификация отправителя на `POST /v1/messages/send` | **Реализована** (расширение области работ, одобренное пользователем). Подпись по версионированной строке `echolet-mailbox-envelope:v1:…` проверяется по `device_pubkey` опубликованного root-signed `DeviceRecord` до записи в mailbox | Неподписанный → 400, чужой ключ → 403, подделка → 403, неопубликованный отправитель → 403 `UNAUTHORIZED_MAILBOX_ACCESS` |
| Квота неподтверждённых конвертов на отправителя | **Реализована** (расширение области работ, одобренное пользователем). `ECHOLET_MAX_UNACKED_ENVELOPES_PER_SENDER`, по умолчанию 16 живых конвертов одного отправителя в одном mailbox; 17-й отвергается `403 SENDER_QUOTA_EXCEEDED` до записи | Слот освобождается при ack и при истечении срока; побайтно идентичный повтор не расходует квоту |
| Сессии | Node-runtime на официальном libsignal 0.102.0 с явным доверием identity и зашифрованным атомарным хранением; используется CLI-прототипом. Старый `crypto-core/session` остаётся demo-only. **Mobile к нему не подключён** | Проверенная зрелая библиотека, сохранение состояния, ответы, restart и задержанная доставка |
| Mobile | `MessagingScreen` содержит демонстрационный путь, закрытый по умолчанию; **6 тестов проверяют только состояние блокировки**. Реальная доставка, рестарт, потеря связи и фоновая работа на устройствах **не проверялись** | Два независимых клиента проходят пользовательский путь без ручной правки данных |
| Класс затопления mailbox | **Закрыт по доставке как граница** (flow 002, измерено на `c302485`): 4 личности и 49 конвертов максимального размера больше не заклинивают mailbox, сообщение доставляется за 2 poll'а. Атака по-прежнему возможна, обход оплачивается один раз | Легитимное сообщение доставляется за затоплением, новая цена атаки измерена и заявлена как граница |
| Устойчивость re-walk к прерыванию | **Реализована и проверена** (flow 002): 32 конфигурации прерывания (SIGINT/SIGKILL, минимальный и максимальный шифротекст), 0 потерь и 0 дублей | Ни Ctrl-C, ни SIGKILL во время восстановительного poll'а не теряют сообщение |
| HTTPS на relay | **Реализовано** (flow 002): пара TLS читается с диска, перечитывается по digest без рестарта, молчаливой деградации в открытый HTTP нет — relay отказывается стартовать | Полусконфигурированный TLS завершает процесс с именем переменной или пути |
| Операторская TUI | **Реализована** (flow 002): профили, очереди, история, отказы и здоровье relay поверх локального CLI; ключи хранилища и приватный ключевой материал не принимаются, не хранятся и не передаются — показано живым прогоном | Ключ не появляется ни в одном кадре, консоль не импортирует профиль, хранилище и криптографию |
| Метаданные | Внешний envelope раскрывает серверу identity/device обеих сторон, время и объём | Не обещать сравнительное снижение утечек без отдельной модели и измерений |
| Безопасность и спрос | Независимый внешний аудит и пользовательский пилот **не проведены** | Отдельные evidence gates; зелёные unit tests не заменяют аудит или спрос |

## Итоговая проверка (T55, независимый верификатор, 2026-09-07)

Среда: macOS (Darwin) arm64, 10 ядер, **Node v26.5.0** (`/opt/homebrew/bin/node`), pnpm 10.0.0, go1.26.1 darwin/arm64, keryx 0.2.80. Целостность файлов подтверждена SHA-256, а не mtime.

- `pnpm test` → **162 теста, зелено в четырёх полных прогонах без фильтрации**, один из них под искусственной нагрузкой в 24 воркера. Разбивка: cli 101, session-node 24, crypto-core 16, protocol 12, mobile 6, client-core 2, client-db 1 (33 файла).
- `pnpm --filter @echolet/cli test` → 22 файла / 101 тест без исключений путей и файлов.
- `pnpm --filter @echolet/cli test:e2e` → **3/3** реальных двухпроцессных прогона через настоящий relay.
- `pnpm typecheck` → PASS для всех проектов.
- `go -C apps/relay test ./...` → PASS; то же с `-race -count=1` и с `-race -count=1 -tags=relayv2`: 5 пакетов `ok`, 0 гонок данных.
- `keryx health run --strict` → gate PASS, оценка проекта 95; `keryx gdgraph query cycles` → циклов нет; `keryx wiki check-links` → 19 страниц, 38 внутренних ссылок, 0 битых.
- Целостность тестов и конфигураций: **59/59 OK** по SHA-256; ни одного `skip`/`only`/`todo`.

Полные измерения: [T55 final verification](../.metaproject/flows/001-2026-09-05-echolet-assessment-fixes/t55-final-verification.md).

Ограничение самих проверок, которое нельзя обходить молчанием: они устанавливают локальное поведение технического прототипа на одной машине macOS arm64 и ничего сверх этого.

## Что изменилось в flow 002 (независимая проверка на `c302485`)

Все числа ниже измерены независимым верификатором на дереве `c302485` против реального relay-бинарника и реального `dist/cli.js`: [T10 verification r3](../.metaproject/flows/002-2026-09-07-echolet-close-the-flood-class-operator-c/t10-verification-report-r3.md). Полная матрица на этом дереве: `pnpm typecheck` — чисто; `pnpm test` — **293 теста в 50 файлах, 293 зелёных**; `pnpm --filter @echolet/cli test:e2e` — **3/3**; `go -C apps/relay test ./...` untagged, с `-race` и с `-race -tags relayv2` — все зелёные, **0 гонок данных**.

- **Класс затопления mailbox закрыт по доставке — как граница, а не как устранение атаки.** Прежний сценарий, который заклинивал mailbox, больше его не заклинивает: атакующий с **4 самостоятельно опубликованными личностями** и **49 конвертами максимального размера** (**12,23 МБ**, **53 запроса**) больше не мешает доставке — легитимное сообщение приходит за **2 poll'а / 17 страниц**, а три последующих обычных poll'а стоят 4 страницы. Читать это следует буквально: атакующий по-прежнему может опубликовать личности и положить конверты, а получатель по-прежнему **один раз** проходит по ним. Закрыто именно **усиление между poll'ами** — благодаря устойчивой позиции чтения отравленные конверты больше не перечитываются, — а это и делало заклинивание постоянным. **Цена одного затопления ограничена, но не равна нулю.**
- **Восстановление после `contact import` (re-walk) стало устойчивым к аварийной остановке.** Верификатор прогнал **32 конфигурации прерывания** (SIGINT и SIGKILL; минимальный и максимальный размер шифротекста; прерывание внутри запроса страницы, внутри `accept()` и вокруг подтверждения) — **ни одного потерянного сообщения и ни одного дубля**; завершённый re-walk не повторяется. До этого исправления Ctrl-C во время восстановительного poll'а молча и необратимо терял сообщение.
- **Relay отдаёт HTTPS и не деградирует молча.** Если задана только одна из двух TLS-переменных или файл отсутствует, повреждён или не соответствует паре, relay завершается ненулевым кодом при старте с именем переменной или пути, а не начинает обслуживать открытый HTTP.
- **Появилась операторская TUI-консоль** поверх локального CLI. Свойство «ключи не покидают терминал» **показано, а не заявлено**: живой прогон реальной TUI против реального relay с настоящим 32-байтовым ключом хранилища в окружении — ключ не появляется ни в одном кадре, а модули консоли не импортируют ни профиль, ни хранилище, ни криптографию.

Что flow 002 **не** установил: половина AC4 «с другой машины» (HTTPS проверен, кросс-машинный прогон — нет: с loopback это недостижимо) и развёртывание AC7, которое заблокировано включением HTTPS Certificates для tailnet — см. §3.1 [deployment runbook](requirements/echolet-cli-prototype/deployment-runbook.md).

## Разбор findings и статус исправлений

Итоговая таблица дисциплинированных решений — [T56 final dispositions](../.metaproject/flows/001-2026-09-05-echolet-assessment-fixes/t56-final-dispositions.md), 110 строк, составлена независимым верификатором, который не писал ни этот код, ни эти ревью.

| Диспозиция | Строк | Из них открыто |
|---|---:|---|
| исправлено | 51 | — |
| исправлено частично | 7 | 7 |
| принято сознательно, с записанной причиной | 26 | 26 — по решению, а не по умолчанию |
| не исправлено / открыто | 21 | 21 |
| задокументированное ограничение | 2 | 2 |
| перекрёстная ссылка на другую строку | 3 | — |

Блокируют приёмку **4 строки, все — один класс дефекта** (T49-F-001, T52-F-001, T53-F-001, T54-F-001: затопление mailbox). Ничего другого не остаётся открытым на уровне blocker или major. Ни один finding за весь flow не был опровергнут.

## Открытые ограничения

1. **Затопление mailbox ограничено по цене, а не устранено как атака** (обновлено в flow 002). Заклинивание mailbox устранено и это измерено — см. раздел flow 002 выше, — но сама атака доступна по-прежнему: `POST /v1/device-records/publish` не аутентифицирован, поэтому личность стоит один бесплатный запрос, ничто не ограничивает число разных отправителей в одном mailbox и ничто не ограничивает занимаемый ими диск. Получатель один раз проходит по всему затоплению (измерено: 17 страниц за 2 poll'а для 49 конвертов максимального размера). Первый проход очень большого затопления остаётся медленным: крайний случай из проектного документа (10 801 конверт, ~2,8 ГБ) даёт первый обход порядка часа, и против 24-часового срока жизни сообщения это не комфортный запас. **Это граница, и она заявляется как граница.**
2. **Один опубликованный bundle обслуживает ровно одного первого отправителя.** У `rotateBundle()` и `retryPending()` нет точки входа в CLI — сознательное решение не ломать замороженную поверхность команд, — поэтому после того как первый отправитель востребовал bundle получателя, второй отличный отправитель получает `PREKEY_BUNDLE_UNAVAILABLE`, а повторная публикация сообщает `claimable: false` вместо восстановления доступности. Ни один критерий приёмки при этом не нарушается: ни один из них не требует, чтобы двое разных отправителей дошли до одного получателя.
3. **Бюджет байт на poll не связан с `ECHOLET_MAX_MESSAGE_BYTES`.** При `maxMessageBytes` = 2 MiB relay вернул ответ на 2 097 781 байт — примерно вдвое больше жёсткого предела клиента, и ничего не было подтверждено. При значении по умолчанию 262144 недостижимо.
4. **Устойчивость backfill.** `BackfillDeviceMailboxBindings` — единственный писатель `device_mailbox` без ограниченного повтора при конфликте, и одна нерасшифрованная запись прерывает весь скан: измерено 0 из 2 здоровых записей связано после одной битой. Стоимость скана при старте ни разу не подтверждена верификатором.
5. **Остатки тестового покрытия.** Курсорный путь `GetEnvelopeBatchFrom`, обслуживающий каждый производственный poll, не имеет теста репозитория с непустым курсором; освобождение бюджета байт для первого конверта переживает удаление; подпись на mobile не наблюдаема ни одним тестом; четыре области исправлений ни разу не были достигнуты мутационным проходом.
6. **Ограничения тестового стенда.** Выше примерно 30-кратной перегрузки CPU vitest теряет результаты целого файла из-за собственного 60-секундного потолка `onTaskUpdate`, и рычага конфигурации для этого нет (при фактической нагрузке гейтов ≤4,8× не наблюдалось ни разу). Строгий health-адаптер не запускает ESLint и не связывает независимо проходящие typecheck и test со своими источниками, поэтому его оценка — совещательная; авторитетным счётом тестов является независимый прогон по workspace.
7. **`ECHOLET_MAX_STORAGE_BYTES` объявлена и нигде не применяется**, ограничения занятости отдельного mailbox тоже нет. Затопление по-прежнему заполняет диск relay без предела; на развёрнутом relay это становится живым классом доступности, а не локальной странностью. Следить за `df -h /var/lib/echolet` придётся вручную — кода, который следит за этим, нет.
8. **Остатки flow 002.** Перечислены в [T16 implementation report](../.metaproject/flows/002-2026-09-07-echolet-close-the-flood-class-operator-c/t16-implementation-report.md) §8 (R-1…R-6: устаревшие комментарии в двух тестовых файлах, ссылающиеся на удалённый метод `takeMailboxRewalk`; три файла, не прошедшие `gofmt`, из которых два — до этого flow; отсутствие сериализации одновременных `poll` по одному каталогу профиля) и в [T10 r3](../.metaproject/flows/002-2026-09-07-echolet-close-the-flood-class-operator-c/t10-verification-report-r3.md) §7 (T10R3V-F-001 — процессная находка, см. ниже; T10R3V-F-002 — неотслеживаемый бинарник `apps/relay/relay` в дереве, не соответствующий ни одному коммиту; T10R3V-F-003 — формулировка «обычный poll оставляет хранилище побайтово идентичным» буквально неверна, меняется служебная запись шифрованного SQLite, поведенческое свойство при этом подтверждено; T10R3V-F-004 — IPv4-mapped IPv6 loopback отвергается, отказ в безопасную сторону).

Полный перечень открытых пунктов flow 001 с измеренной ценой — в [итоговом отчёте об изменениях](../.metaproject/flows/001-2026-09-05-echolet-assessment-fixes/final-change-report.md).

## Предупреждение о среде выполнения

`apps/cli` объявляет `"engines": {"node": ">=22.13"}`. На **Node 22.12 и ниже отсутствует `node:sqlite`**, и тогда большинство CLI-наборов не собирается: на момент измерения 13 из 17 наборов падали на сборке, а процессные наборы завершались с кодом 1 без вывода. **Эта картина неотличима от сломанной реализации, но ею не является.** Постоянного пина версии не добавлено, поэтому предупреждение остаётся актуальным для следующего читателя. Все зафиксированные результаты получены на Node 26.5.0.

## Решения пользователя, зафиксированные в этом flow

- 2026-09-06: пользователь утвердил отревьюированный пакет требований `docs/requirements/echolet-cli-prototype/` и запросил автономную реализацию через flow-orchestrator с управляемыми субагентами.
- 2026-09-07: **аутентификация отправителя** на `/v1/messages/send` — расширение области работ, выбранное пользователем явно, после того как оркестратор показал измеренную цену атаки и конфликт двух стоящих инструкций (закрыть все blocker/major против «не менять область прототипа»). Квота на отправителя следует из того же решения после того, как проверка показала, что одной аутентификации недостаточно.
- 2026-09-07: получив измеренные числа остаточной атаки, пользователь выбрал **зафиксировать затопление mailbox как задокументированное ограничение**, а не расширять область работ дальше.

## Исправлено в flow 001

- Строгий Signal wire v2 с Kyber и подписью полного bundle ключом устройства; root-signed `DeviceRecord` связывает устройство с ожидаемой identity, native prekey signatures проверяются до импорта, import не меняет trust. [Документ](PROTOCOL-30_SIGNAL_BUNDLE_V2.md).
- Node-only session runtime на официальном libsignal 0.102.0: явное доверие identity, зашифрованное атомарное хранение session/inbox/outbox, точный повтор. Проверены SIGKILL до и после commit, rollback при ошибке COMMIT и восстановление той же очереди.
- CLI-прототип: профили, экспорт и импорт контактных карточек с закреплением Signal identity, публикация bundle, отправка, приём, история и `doctor`; типизированные коды выхода 0/2/3/4/5.
- Relay: конфликт `envelope_id` возвращает 409 вместо перезаписи; истёкшие записи отбрасываются до применения лимитов; каждый v1-декодер ограничен по размеру тела; `size_bytes` сверяется с фактической длиной шифротекста; идентификаторы ограничены по длине; авторизация mailbox опирается на неизменяемую связку `device_mailbox`, а не на обратный поиск по устройству.
- Relay: выдача батча ограничена одновременно количеством и байтами, при этом один конверт максимального размера выдаётся всегда; `next_cursor` стал позицией возобновления, поэтому отвергнутые конверты больше не удерживают окно выборки навсегда; решение о лимите частоты принимается без ввода-вывода под блокировкой, а ключ квоты нормализуется без учёта порта и клиентских заголовков.
- CLI: повторное чтение outbox внутри транзакции мутации; типизированная `PersistenceError`, отличаемая по происхождению, а не по тексту; поконвертное принятие с агрегированным списком `rejected`; ограниченный обход страниц; строгий разбор аргументов сохранён при поддержке значений, начинающихся с дефиса.
- Аутентификация отправителя и квота неподтверждённых конвертов на отправителя (см. решения пользователя выше).
- Messaging на mobile по умолчанию закрыт: требуется `__DEV__ === true` плюс явный `EXPO_PUBLIC_ECHOLET_ENABLE_UNSAFE_DEMO=true`, release не может включить режим через env. Сам криптографический протокол этим не исправлен.
- Повторное использование mailbox challenge конкурентными poll-запросами устранено атомарным consume после проверки подписи и владельца.
- HTTP/TLS и mailbox signing contracts согласованы с кодом; необоснованное сравнительное обещание защиты метаданных удалено.
- Закрыт долг документации по контракту relay/CLI: `claimable`, поле запроса `cursor` и семантика позиции `next_cursor`, форма результата `poll` `{received, more, rejected}`, поле `sender_signature` и его транскрипт, квота на отправителя и коды `PREKEY_BUNDLE_UNAVAILABLE`, `UNAUTHORIZED_MAILBOX_ACCESS`, `SENDER_QUOTA_EXCEEDED` с отображением на коды выхода — [API-11](API-11_JSON_SCHEMAS.md), [PROTOCOL-07](PROTOCOL-07_MVP_MESSAGE_FLOW.md), [спецификация прототипа](requirements/echolet-cli-prototype/specification.md).

## Что не установлено этими проверками

Ни один пункт этого списка не смягчён результатами flow 002. Появление операторской TUI, HTTPS на relay и измеренное закрытие класса затопления по доставке **не** создают ничего из перечисленного:

- производственная безопасность и пригодность для чувствительной переписки;
- доставка на мобильных устройствах: `apps/mobile` даёт 6 тестов, проверяющих только состояние блокировки; ни обмена на реальных устройствах, ни рестарта, ни потери связи, ни фоновой доставки не выполнялось. Изменение подписи на mobile корректно по построению, но не наблюдаемо ни одним тестом;
- публичное развёртывание и готовность к промышленной эксплуатации: развёртывание relay на tailnet — прототипное и на момент этой редакции **не выполнено**;
- завершение независимого криптографического аудита: все проверки на записи — внутренние агентные верификации, аудита не было;
- пользовательский спрос и пилот: интервью и пилот не проводились ([PILOT-29](PILOT-29_VALIDATION_PLAN.md) хранит пустые поля evidence);
- постоянная пригодность зафиксированной зависимости `@signalapp/libsignal-client@0.102.0`, которая взята только для прототипа.

Разрешённое утверждение после прохождения гейта, дословно из [metrics-and-validation.md](requirements/echolet-cli-prototype/metrics-and-validation.md): два локальных компьютерных CLI-клиента обменялись зашифрованным текстом через relay Echolet и восстановились после проверенных перезапусков. К нему прилагается исключение, обновлённое по flow 002: mailbox больше **не заклинивается** самостоятельно опубликованным отправителем, затапливающим офлайн-получателя (измерено на `c302485`), но сам он от такого отправителя **не защищён** — затопление по-прежнему возможно, оплачивается получателем один раз и ничем не ограничено по занимаемому на relay диску.

## Независимая проверка изменений

Внутренние агентные ревью и верификации, не внешний аудит: [верификация flow 002 на `c302485` (T10 r3)](../.metaproject/flows/002-2026-09-07-echolet-close-the-flood-class-operator-c/t10-verification-report-r3.md), [обследование хостов T11](../.metaproject/flows/002-2026-09-07-echolet-close-the-flood-class-operator-c/t11-host-recon.md), [итоговая верификация T55](../.metaproject/flows/001-2026-09-05-echolet-assessment-fixes/t55-final-verification.md), [итоговые диспозиции T56](../.metaproject/flows/001-2026-09-05-echolet-assessment-fixes/t56-final-dispositions.md), [консолидированное ревью T28](../.metaproject/flows/001-2026-09-05-echolet-assessment-fixes/t28-review-report.md), [SignalClient review](../.metaproject/flows/001-2026-09-05-echolet-assessment-fixes/signal-client-independent-review.md), [storage review](../.metaproject/flows/001-2026-09-05-echolet-assessment-fixes/storage-review.md), [ревью mobile guard и atomic challenge](../.metaproject/flows/001-2026-09-05-echolet-assessment-fixes/independent-review.md).

## Исторические результаты

Апрельский [отчёт об исправлениях](../jobs/task--echolet-fix-critical-review-findings/man/final-report.md) фиксирует проходившие тогда проверки и оставшиеся архитектурные пробелы; он не является свежей аттестацией текущих файлов. [Предыдущий STATUS_CURRENT](../.metaproject/flows/001-2026-09-05-echolet-assessment-fixes/status-before/STATUS_CURRENT.md) сохранён без изменения. [Первая волна](../.metaproject/flows/001-2026-09-05-echolet-assessment-fixes/change-report.md), [session-волна](../.metaproject/flows/001-2026-09-05-echolet-assessment-fixes/official-session-change-report.md) и [wire adapter](../.metaproject/flows/001-2026-09-05-echolet-assessment-fixes/wire-adapter-report.md) сохранены отдельно.

## Ближайшие gates

1. Закрыть корень класса затопления: аутентифицировать `POST /v1/device-records/publish` либо ограничить занятость mailbox целиком, а не только одного отправителя. Доставка уже не блокируется, но цена атаки для атакующего осталась прежней, а `ECHOLET_MAX_STORAGE_BYTES` не применяется.
2. Реализовать собственный mobile bridge и защищённое хранение ключа базы на проверяемых контрактах Node reference; подтвердить обмен, рестарт, потерю связи и фоновую доставку на реальных iOS и Android.
3. Провести независимый внешний криптографический аудит.
4. Выполнить пилот по [PILOT-29](PILOT-29_VALIDATION_PLAN.md), записать фактические результаты и решение о продолжении.
5. Включить HTTPS Certificates для tailnet `tail5a88fb.ts.net` (админ-консоль Tailscale → DNS → HTTPS Certificates) — это владелец tailnet и только он; без этого `tailscale cert` не выдаст пару, а relay не стартует. После этого развернуть два relay по [deployment runbook](requirements/echolet-cli-prototype/deployment-runbook.md) и закрыть кросс-машинную половину AC4.

Количество файлов, зелёные unit tests и завершение отдельных flow tasks не означают принятия MVP. Требуется выполнение [критериев приёмки](TEST-16_ACCEPTANCE_CHECKLIST.md).
