# v0.1.1

## Station UI and contact cards

- Fixed repeated contact-card exports failing with `PERSISTENCE_FAILURE`. Every request now uses a unique temporary file and removes it afterward.
- Added file/JSON import with bounded input, server-side cryptographic validation, preview, and explicit confirmation.
- Added actionable empty-room controls and consistent Russian action labels across all six station skins.
- Distinguished local-station connectivity, authenticated relay status, and relay HTTP reachability. Preserved the station label across live updates.
- Improved skin contrast, responsive layouts, keyboard controls, dialog focus, and reduced-motion behavior.
- Preserved input after errors and rejected stale import, export, and status responses.
- Passed the selected profile's relay URL and store-key environment to the station process.
- Fixed the CLI version declaration's TypeScript scope error.

## Verification

Focused station tests, real-CLI card exports, non-mutating validation, confirmed import, HTTP/SSE smoke checks, web build, and web TypeScript checks passed before release preparation. Full repository checks run through the push gate and release workflow.

Known limitation: the updated six skins have not completed the final manual browser pass for narrow screens, zoom, and keyboard interaction because the browser controller was unavailable. Automated tests do not substitute for that visual verification.
